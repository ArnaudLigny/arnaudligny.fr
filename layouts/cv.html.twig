{% extends 'page.html.twig' %}

{% block content %}
	<article class="page typeset">
		<h1>{{ page.title }}</h1>
    <img src="/logo-300.jpg" width="150" height="150" class="profile-picture" alt="Photo de profil d'{{ site.author.firstname ~ ' ' ~ site.author.lastname }}"/>
    {{ page.intro|markdown_to_html }}
    {{ site.pages.all['index'].content|markdown_to_html }}
    {{- page.content ~}}
	</article>
{% endblock content %}

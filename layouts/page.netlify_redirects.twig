{% for page in site.pages if page.redirect is defined %}
{{ url(page, {canonical: false}) }}    {{ url(page.redirect, {canonical: false}) }}    301!
{% endfor %}
# Redirect default Netlify subdomain to primary domain
https://arnaudligny.netlify.com/* https://arnaudligny.fr/:splat 301!

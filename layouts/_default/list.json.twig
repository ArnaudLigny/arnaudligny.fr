{% extends '_default/page.json.twig' %}

{% set items = page.pages|sort_by_date %}

{# disable pagination
{% if page.pagination.pages is defined %}
{% set pagination = page.pagination %}
{% set items = pagination.pages %}
{% endif %}
#}

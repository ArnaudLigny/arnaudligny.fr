{% extends ['page.html.twig', '_default/page.html.twig'] %}

        {%- block content ~%}
        <article class="page typeset">
          <h1>{{ page.title }}</h1>
          {{- page.content ~}}
        </article>
        {%- include 'partials/lastposts.html.twig' ~%}
        {%- include 'partials/lasttalks.html.twig' ~%}
        {%- endblock content ~%}
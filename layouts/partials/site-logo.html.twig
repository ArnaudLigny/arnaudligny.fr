        {#- logo ~#}
        {%- set logo = asset(site.logo)|webp ~%}
        <a class="logo" href="{{ url(site.home) }}">
          <img src="{{ logo|dataurl }}" width="{{ logo.width }}" height="{{ logo.height }}" alt="{{ site.title }} logo">
        </a>
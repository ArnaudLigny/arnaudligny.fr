<nav class="nav">
  <ul class="list list--nav">
{% for item in site.menus.main %}
    <li class="item item--nav{% if url(item.url) == url(page) %} item--current{% endif %}">
        <a href="{{ url(item.url) }}">{{ item.name }}</a>
    </li>
{% endfor %}
  </ul>
</nav>
